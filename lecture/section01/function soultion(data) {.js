function soultion(data) {
    let min = data[0];
    let sum = 0;
    for (let i=0; i<data.length; i++) {
        if(data[i]% 2 !== 0) {
            // 홀수 일 경우

            sum += data[i];

        }
    }
}